import "../index.css";
import Router from "./Router.jsx";

function App() {
  return <Router />;
}

export default App;

// Do the fetch
// Create & Provide the context
// Render the header (with passed context)
// Render the Router
